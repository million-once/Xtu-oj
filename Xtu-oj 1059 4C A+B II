//难点1：前导空格递减 我们要让最后一行没有空格，第一行空格最多，所以空格数 = 最大空格数 - 当前行号。
//难点2：如何同时处理各个输入

#include <stdio.h>

int main(){
    int n;
    
    while (scanf("%d", &n) == 1 && n != 0) {    //重点：同时进行输入和判断并且进入循环 直接处理，不存储
    
        for (int i = 0; i < n-1; i++)    //递增，前导空格长度为n-1
            {
                printf(" ");    //作出第一行的前导空格
            }
        
        printf("1\n");    //第一行的1
    
        for (int i = 1; i < n; i++)
            {
                for (int j = 0; j < n-1-i; j++)    //前导空格递减 我们要让最后一行没有空格，第一行空格最多，所以空格数 = 最大空格数（n-1） - 当前行号。
                    {
                        printf(" ");    //作出当前行的前导空格
                    }
                for (int j = 1; j <= i; j++)
                    {
                        printf("%d",j);    //作出+号左边的递增数列
                    }
                printf("+");    //作出加号
                
                for (int j = i; j >= 1; j--)
                    {
                        printf("%d",j);    //作出+号右边的递减数列
                    }
                
                printf("\n");    //每行换行
            }
        }

    return 0;
}
